<h1>Task Overview</h1>
<% @tasks.each do |task| %>
  <%= link_to task.name, task_path(task) %>
  <%= link_to "destroy", task_path(task),
  method: :delete,
  data: { confirm: "Are you sure?" } %>
<% end %>
